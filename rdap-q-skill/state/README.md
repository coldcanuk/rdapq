# RDAP-Q Repository State Templates

Copy these templates into:

```text
<repo>/.rdapq/state/
```

Repository state is active working memory for the current project/build.

It is not the same as durable RDAP-Q memory under `$RDAPQ_HOME`.

Keep state concise and evidence-linked.

Do not place secret values in state files.
